# Frontend (Next.js) environment.
# Copy to .env.local and adjust for your setup.

# Base URL of the backend API. Defaults to http://localhost:5000/api if unset.
NEXT_PUBLIC_API_URL=http://localhost:5000/api
