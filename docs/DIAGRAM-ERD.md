# Entity-Relationship Diagram (ERD)

Generated from the actual Mongoose models in `server/models`. It shows the 15
collections of the Memory Assistant System and their relationships.

**How to use:** paste the code block below into <https://mermaid.live>, then
export as PNG/SVG and insert into the thesis (or redraw in draw.io if strict UML
notation is required).

```mermaid
erDiagram
    USER ||--o| PATIENT : "has profile"
    USER ||--o| CAREGIVER : "has profile"
    CAREGIVER }o--o{ PATIENT : "assigned to"

    PATIENT ||--o{ MEDICATION : has
    PATIENT ||--o{ ROUTINE : has
    PATIENT ||--o{ MEDICATIONLOG : records
    PATIENT ||--o{ ROUTINELOG : records
    PATIENT ||--o{ ALERT : raises
    PATIENT ||--o{ CHATHISTORY : has
    PATIENT ||--o{ KNOWNFACE : has
    PATIENT ||--o{ MEMORY : has
    PATIENT ||--o{ REPORT : has
    PATIENT ||--o{ NOTE : has
    PATIENT ||--o{ RECOGNITIONLOG : has
    PATIENT ||--o{ REMINDER : has

    MEDICATION ||--o{ MEDICATIONLOG : "is logged in"
    ROUTINE ||--o{ ROUTINELOG : "is logged in"

    USER ||--o{ MEDICATION : "added by"
    USER ||--o{ ROUTINE : "added by"
    USER ||--o{ ALERT : "notified caregiver"
    USER ||--o{ NOTE : "written by"
    USER ||--o{ REPORT : "generated by"
    USER ||--o{ KNOWNFACE : "added by"
    USER ||--o{ MEMORY : "added by"

    USER {
        ObjectId _id
        string name
        string email
        string password
        string phone
        string role
        boolean isActive
    }
    PATIENT {
        ObjectId _id
        ObjectId user FK
        date dateOfBirth
        string gender
        string cnic
        string address
        string city
        string diagnosis
        string doctor
        string bloodGroup
        array allergies
        array emergencyContacts
        array assignedCaregivers FK
    }
    CAREGIVER {
        ObjectId _id
        ObjectId user FK
        string specialization
        array assignedPatients FK
        string notes
    }
    MEDICATION {
        ObjectId _id
        ObjectId patient FK
        string name
        string dosage
        string frequency
        array times
        string instructions
        date startDate
        date endDate
        ObjectId addedBy FK
        boolean isActive
    }
    MEDICATIONLOG {
        ObjectId _id
        ObjectId medication FK
        ObjectId patient FK
        date scheduledTime
        string status
        date takenAt
        string notes
    }
    ROUTINE {
        ObjectId _id
        ObjectId patient FK
        string activityName
        string description
        string startTime
        string endTime
        array days
        string priority
        ObjectId addedBy FK
        boolean isActive
    }
    ROUTINELOG {
        ObjectId _id
        ObjectId routine FK
        ObjectId patient FK
        date scheduledDate
        string status
        date completedAt
        string notes
    }
    ALERT {
        ObjectId _id
        ObjectId patient FK
        ObjectId caregiver FK
        string type
        string severity
        string message
        boolean isResolved
        ObjectId resolvedBy FK
        date resolvedAt
    }
    CHATHISTORY {
        ObjectId _id
        ObjectId patient FK
        string mode
        string query
        string response
        string intent
        number confidence
    }
    KNOWNFACE {
        ObjectId _id
        ObjectId patient FK
        string name
        string relationship
        string imageUrl
        array descriptor
        string phone
        ObjectId addedBy FK
        number recognitionCount
        date lastSeen
    }
    RECOGNITIONLOG {
        ObjectId _id
        ObjectId patient FK
        string imageUrl
        string result
        object recognizedPerson
        number confidence
    }
    MEMORY {
        ObjectId _id
        ObjectId patient FK
        string title
        string imageUrl
        array people
        string location
        date date
        string description
        ObjectId addedBy FK
    }
    REPORT {
        ObjectId _id
        ObjectId generatedBy FK
        ObjectId patient FK
        string type
        string title
        object period
        string format
        string filePath
        string status
        object data
    }
    NOTE {
        ObjectId _id
        ObjectId patient FK
        ObjectId caregiver FK
        string content
    }
    REMINDER {
        ObjectId _id
        ObjectId patient FK
        string type
        ObjectId referenceId
        date scheduledTime
        string message
        boolean isDelivered
        date deliveredAt
    }
    CONTACT {
        ObjectId _id
        string name
        string email
        string subject
        string message
    }
```

## Description

- **USER** is the base account for all roles. A **PATIENT** or **CAREGIVER**
  record extends a user with role-specific data (one-to-one with USER).
- **CAREGIVER** and **PATIENT** have a **many-to-many** relationship: a caregiver
  can be assigned several patients, and a patient can have several caregivers
  (stored as `assignedPatients` / `assignedCaregivers`).
- A **PATIENT** owns most operational data: medications, routines, their logs,
  alerts, chat history, known faces, memories, reports, notes, recognition logs,
  and reminders.
- **MEDICATION → MEDICATIONLOG** and **ROUTINE → ROUTINELOG** record each
  scheduled item's taken/missed/completed status over time.
- **USER** also appears as `addedBy` / `generatedBy` / `caregiver` on several
  records to track who created or is responsible for them.
- **CONTACT** is a standalone entity for public contact-form messages.

> Note: MongoDB is schema-flexible; the FK markers denote Mongoose `ObjectId`
> references (`ref`) rather than enforced relational foreign keys.
